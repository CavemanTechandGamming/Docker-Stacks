# Locally built image tag (see compose.yaml — run `docker compose build` before `up -d`)
# NUT_SLAVE_IMAGE_TAG=local

# Folder that holds upsmon.conf (copy upsmon.slave.example → ./local/upsmon.conf)
# NUT_SLAVE_LOCAL_DIR=./local
