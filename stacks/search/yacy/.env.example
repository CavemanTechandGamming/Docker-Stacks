# HTTP + HTTPS listeners inside the container; map only what you need on the host
YACY_PUBLISH_HTTP=8090
YACY_PUBLISH_HTTPS=8443

# Pin or bump when upgrading — see https://hub.docker.com/r/yacy/yacy_search_server/tags
YACY_VERSION=1.93
