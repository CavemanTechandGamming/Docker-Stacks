# Public URL users type in the browser (used by SearXNG for links and CSRF). Include scheme and port if non-80/443.
SEARXNG_BASE_URL=http://localhost:8088

# Host port → container 8080
SEARXNG_PUBLISH_PORT=8088

# SEARXNG_VERSION=latest
