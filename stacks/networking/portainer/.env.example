# Image tag (pin for production)
PORTAINER_IMAGE=portainer/portainer-ce:latest

# Web UI (HTTPS). First visit creates the admin user.
PORTAINER_HTTPS_PORT=9443

# Docker socket on the host. Default works on Linux and typical Docker Desktop (WSL2) setups.
# DOCKER_SOCK=/var/run/docker.sock
