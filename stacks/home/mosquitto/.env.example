# Official image tag — bump intentionally after reading upstream release notes.
MOSQUITTO_IMAGE=eclipse-mosquitto:2

# MQTT (plain TCP).
MOSQUITTO_PORT=1883

# Optional WebSockets — enable listener in config/mosquitto.conf and uncomment ports in compose.yaml first.
# MOSQUITTO_WS_PORT=9001

MOSQUITTO_DATA_DIR=./data
